# INTRODUCTION

## directory

```
a2a
  📦 packages
    📂 cli
      📋 将本地.json转化为.dart/.ts实体
      🔧 commander
      🔧 chokidar      
    📂 core
      📋 json编译器，将json解析为ast
      🔧 parser 语法解析器
      🔧 tokenizer 词法解析器
      🔧 transformer 转化器
    📂 json2dart
      🔧 next.js + react + antd
    📂 json2ts
      🔧 vite + react + material-ui
    📂 proto2any
      🔧 vite + vue3
    📂 sdk
      📋 目标语言生成器，将json ast转化为ts dart代码
      🔧 vite
  🔧 pnpm + eslint + prettier + commitlint + stylelint + jest
```
