{
  "name": "json2any",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "dev:dart": "cd packages/json2dart && yarn dev",
    "dev:pson": "cd packages/pson && yarn dev",
    "build:dart": "cd packages/json2dart && yarn build",
    "build:pson": "cd packages/pson && yarn build"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
