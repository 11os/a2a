{
  "name": "proto2any",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy:surge:ts": "yarn build && surge ./dist/ proto2ts.surge.sh",
    "deploy:surge:dart": "yarn build && surge ./dist/ proto2dart.surge.sh",
    "deploy:surge": "yarn deploy:surge:ts && yarn deploy:surge:dart"
  },
  "dependencies": {
    "clipboard": "^2.0.8",
    "protobufjs": "^6.10.2",
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.1.5",
    "@vue/compiler-sfc": "^3.0.5",
    "typescript": "^4.1.3",
    "vite": "^2.2.4"
  }
}
